               　　　ICS to CSV コンバータ
                            ics2gacsv
                      インストール手順書

                                                            2025/9/24
                                                (最終更新) 2025/12/12

                                                          by 松元隆二

はじめにREADME.txtを読んでください。

*ソフトウエアのダウンロード

以下から最新版をzipファイルもしくはtar.gzファイルでダウンロードして
展開してください。

    https://github.com/githubmatumoto/ics2gacsv/releases/

ソフトウエアを展開したフォルダを覚えておいてください。ファイル
「INSTALL.txt」や「isc2gacsv.py」が含まれるフォルダです。

*確認環境

- Ubuntu22.04.5LTS/日本語環境
  OS付属Pythonで確認しています。

  $ python --version
  Python 3.10.12

- Windows11Pro /日本語環境
  MicrosoftStore版Python3.13を導入して確認しています。

  > python3 --version
  Python 3.13.9

- macOS 26.0.1/ARM/日本語環境
  (IntelMacは未対応。動作確認機材無いため)

  上記のOS付属Pythonで確認しています。Pythonが入ってない場合はコマンド
  プロンプトで「python3」と入力すると、OS標準のPythonの入手方法が表示
  されますので、それにしたがってください。

  % python3 --version
  Python 3.9.6

- (非対応)AlmaLinux8.10/日本語環境
  OS付属Pythonは3.6なので動作しません。

  $ python3 --version
  Python 3.6.8  (OS標準Pythonは非対応)

  但し、OSの特権ユーザ等に相談頂いてPython3.9以降を導入すれば恐らく動
  作すると思います。

  $ sudo dnf install python3.11

  $ rpm -qf /bin/python3.11
  python3.11-3.11.13-2.el8_10.x86_64

  $ python3.11 --version
  Python 3.11.13 (本ソフトウエアが対応するPython)

*依存ライブラリvobjectのインストール。

ライブラリ vobjectを使わせていただいてます。バージョンは0.99で動作確認
しています。本ライブラリはPython3.8以降対応になります。

Ref: https://vobject.readthedocs.io/latest/
     https://py-vobject.github.io/
     https://github.com/py-vobject/vobject/releases

**Windows(MicrosoftStore版Python3.13)

コマンドプロンプトでpython3とpip3がであることを確認ください。

  >python3 --version
  Python 3.13.9

  >pip3 --version
  pip 25.2 from C:\Program Files\WindowsApps\(省略)\pip (python 3.13)

  >pip3 install vobject
  (メッセージ省略)

成功したら下記のような表示になります。
  Successfully installed python-dateutil-2.9.0.post0 pytz-2025.2 six-1.17.0 vobject-0.9.9

**Linux/macOSの設定

Linux/macOSではvenvの設定がお勧めです。

前述していますが、pythonのバージョンは3.9以上である必要があります。

venv初期設定(コマンド名python3はOSによって異なります)
  $ python3 -m venv .ics2gacsv --prompt ics2gacsv

venv有効化
  $ source ~/.ics2gacsv/bin/activate

  $ pip3 install vobject
  (メッセージ省略)

成功したら下記のような表示になります。
  Successfully installed python-dateutil-2.9.0.post0 pytz-2025.2 six-1.17.0 vobject-0.9.9

※もしvobjectのバージョンが0.9.9より大きい数字になってた場合は、連絡頂
けると幸いです。

*ics2gacsvの動作確認

ソフトウエアを展開したフォルダにcdで移動してください。

  $ cd "ソフトウエアを展開したフォルダ"

以下のコマンドを実行してください。引数「-h」はヘルプメッセージを出力します。

  $ python3 ics2gacsv.py -h

正常に動作すると、下記のようなヘルプメッセージが出力されます。

   ---
   Help on module ics2gacsv:

   NAME
       ics2gacsv - ICS(iCalendar)をCSVに変換する。CSVの出力形式はGaroonと同じ。

   DESCRIPTION
       使用方法:
           $ python3 ics2gacsv.py [-hubstoOdpmT] 期間 入力.ics 出力.csv
   ---

ヘルプの停止は「q」を押してください。

                                                              以上です。
